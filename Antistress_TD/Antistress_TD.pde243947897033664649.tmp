Button test;

void setup() {
  fullScreen();
  rectMode(CORNERS);
  test = new DragButton(50, 50, 250, 150, "test123");
}

void draw() {
  if (test.collision()) cursor(HAND);
  else cursor(ARROW);
  test.display();
}
